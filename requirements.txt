# Core experiment runtime (required)
psychopy==2025.2.4
sounddevice>=0.4.0
scipy>=1.10.0
numpy>=1.24.0

# Post-processing pipeline (scripts/process.py)
webrtcvad-wheels>=2.0.10       # VAD on vowel trials
opensmile>=2.5.0             # ComParE_2016 acoustic features
pandas>=2.0.0                # openSMILE DataFrame output

# Future / placeholder dependencies
# torch>=2.0.0                 # speaker diarization (not yet wired)
# torchaudio>=2.0.0            # speaker diarization (not yet wired)

# Development (optional)
# pytest>=7.0.0
# ruff>=0.6.0
